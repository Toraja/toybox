# OpenCode

## Config

### MCP

`enabled` fields are all set to `false` by default, as the commands run automatically when opencode starts.  
Run `/mcps` command to toggle any of the MCPs defined in the config.
