# --- Selective checkout with sparse-checkout ---

# setup
git init
git remote add origin <URL>
git config core.sparseCheckout true
git sparse-checkout init
## Specify path to checkout
git sparse-checkout set [path...]
git pull origin <branch to checkout>

# List checked out stuff
git sparse-checkout list

# Disable
git sparse-checkout disable

# --- View files ---

# View file content at the commit / in the branch
git show <commitish>:<path>

# --- Diff ---

# List modified files for a commit
git diff-tree --no-commit-id --name-only -r <commit>

# --- Remote repo ---

# Set upstream automatically
## In .gitconfig, add:
[push]
	default = current
## Without -u option, git pull complains that it does not know where to pull from.
git push -u

# Delete
git push origin :<branch>

# --- Tag ---

# git complains that it "would clobber existing tag"
git fetch --tags -f

# List
## Tags that matches the pattern
git tag [-l (pattern)]
## Tags and their hash (not the commit hash the tags are attached to)
git show-ref --tags

# --- Chores ---

# Shrink .git directory
## ref: https://stackoverflow.com/questions/5613345/how-to-shrink-the-git-folder

## Run this command if lfs is used
git lfs prune

## Shrink .git/objects
git gc
git prune
