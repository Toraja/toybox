require('ftplugin.yaml')
-- vim.wo.foldlevel = 2

-- local surround_config = require('nvim-surround.config')
-- require('nvim-surround').buffer_setup({
--   surrounds = {
--     ["v"] = {
--       add = { "{{ ", " }}" },
--       -- XXX find and delete are not working
--       find = function()
--         return surround_config.get_selection({ motion = "a{{" })
--       end,
--       delete = "^(. ?)().-( ?.)()$"
--     },
--   }
-- })
